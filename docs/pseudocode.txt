get list of categories for main directory
    walk the directory folder, if a folder is a category folder, add it to the list
    return a list of folders

create a dict of {category: articles}
    for category in categories:
        for the given category, walk the files in that folder and get the title and filename from each file: append (title, filename) to [titles]
        categorydict[category] = [titles]

create the index md file
    write the header information (anything that is static)
    write a bullet list of categories, with each item being a link to the folder for that category
    write a full list in the format:
    CATEGORY
        - Title 1
        - Title 2
    write the footer information

